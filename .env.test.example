# Copy to .env.test.local and fill in your test project values from
# Supabase Dashboard → test project → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
APP_BASE_URL=http://localhost:3000

# Prevent email crons from sending on test (set in Vercel env for test project)
DISABLE_EMAIL_CRON=true
